{% extends 'base.html.twig' %}

{% block title %}Demandes de rôles{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="fw-bold text-primary">
            📋 Gestion des demandes de rôles
        </h1>
        <a href="{{ path('admin_roles_creation') }}" class="btn btn-success">
            ➕ Créer un nouveau rôle
        </a>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <table class="table table-hover align-middle">
                <thead class="table-primary">
                    <tr class="text-center">
                        <th>👤 Utilisateur</th>
                        <th>🎭 Rôle demandé</th>
                        <th>📅 Date de demande</th>
                        <th>📌 Statut</th>
                        <th>⚙️ Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for req in requests %}
                        <tr class="text-center">
                            <td>{{ req.getUser().email ?? '—' }}</td>
                            <td class="fw-semibold">{{ req.getRole().role }}</td>
                            <td>{{ req.dateDemande|date('d/m/Y H:i') }}</td>
                            <td>
                                {% if req.status == 'attente' %}
                                    <span class="badge bg-warning text-dark px-3 py-2">🕒 En attente</span>
                                {% elseif req.status == 'approuvee' %}
                                    <span class="badge bg-success px-3 py-2">✅ Approuvée</span>
                                {% else %}
                                    <span class="badge bg-danger px-3 py-2">❌ Rejetée</span>
                                {% endif %}
                            </td>
                            <td>
                                {% if req.status == 'attente' %}
                                    <form method="post" 
                                          action="{{ path('RoleApprouvee', { id: req.id }) }}" 
                                          class="d-inline">
                                        <input type="hidden" name="_token" value="{{ csrf_token('approve' ~ req.id) }}">
                                        <button class="btn btn-success btn-sm">
                                            ✅ Approuver
                                        </button>
                                    </form>

                                    <form method="post" 
                                          action="{{ path('RoleRejetee', { id: req.id }) }}" 
                                          class="d-inline ms-2">
                                        <input type="hidden" name="_token" value="{{ csrf_token('reject' ~ req.id) }}">
                                        <button class="btn btn-danger btn-sm">
                                            ❌ Rejeter
                                        </button>
                                    </form>
                                {% else %}
                                    <span class="text-muted fst-italic">Aucune action disponible</span>
                                {% endif %}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="5" class="text-center text-muted py-4">
                                Aucune demande de rôle pour le moment.
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <div class="text-center mt-4">
        <a href="{{ path('admin_roles_index') }}" class="btn btn-outline-primary">
            ← Retour à la gestion des rôles
        </a>
    </div>
</div>
{% endblock %}
